# Sevenseven Site

Sevenseven 的公开数字工作台，使用纯静态 HTML/CSS 构建，并由 GitHub Pages 发布。

## 本地预览

在项目目录运行：

```powershell
npx --yes serve .
```

然后访问终端显示的本地地址。

## 发布方式

推送到 `main` 分支后，GitHub Pages 会从仓库根目录自动发布。自定义域名为 `sevenseven.qzz.io`。
